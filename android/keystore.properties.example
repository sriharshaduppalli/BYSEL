# Copy this file to `keystore.properties` in the android/ folder and fill values.
# DO NOT commit your real keystore or secrets.
# Example values:
storeFile=keystore.jks
storePassword=CHANGEME_STORE_PASSWORD
keyAlias=upload
keyPassword=CHANGEME_KEY_PASSWORD
